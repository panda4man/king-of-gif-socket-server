{
  "name": "survive-the-giph-socketio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist/ && babel ./src --out-dir dist/ --ignore ./node_modules",
    "start": "npm run build && node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "collect.js": "^3.1.2",
    "crypto-random-string": "^1.0.0",
    "express": "^4.16.2",
    "rimraf": "^2.6.2",
    "socket.io": "^2.0.4"
  },
  "engines": {
    "node": "8.9.2"
  }
}
